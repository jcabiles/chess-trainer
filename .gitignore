# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local config
*.local
.claude/settings.local.json
.claude/*.bak*
SETUP-SANDBOX.md

# Dev/debug artifacts
.playwright-mcp/
board-debug.png
layout-check.png
setup-mode.png

# Local dev-server runtime (managed by scripts/serve.sh)
.server.pid
.server.log

# Game-review feature: user data (never commit), but keep the drop-folder
data/games.db
data/games/*
!data/games/.gitkeep

# Realism audit (M3): raw lichess stream cache — never committed. The small
# derived audit sets (data/realism/*.jsonl + EVAL_SHA256) ARE committed.
data/lichess/

# Local machine-specific helper script (hardcoded ~/Downloads paths)
scripts/bulk_import.py

# Bot avatars: user-supplied portraits (never commit; app falls back to initials)
data/avatars/
